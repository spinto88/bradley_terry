name: Rebuild visualization data

on:
  push:
    branches:
      - main
    paths:
      - 'data/**'
      - 'src/**'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run build script
        run: |
          python -m src.build_site_data

      - name: Commit updated JSON
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add docs/data/infered_score.json docs/data/bootstrap.json docs/data/results.json
          git commit -m "Auto-update visualization data" || echo "No changes"
          git push origin HEAD:main
